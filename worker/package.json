{
  "name": "@wwwshare/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.49",
    "vitest": "^3.2.4",
    "wrangler": "^4.90.1"
  }
}
