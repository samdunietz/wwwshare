{
  "name": "wwwshare",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["worker", "cli"],
  "scripts": {
    "dev": "npm run dev --workspace=@wwwshare/worker",
    "test": "npm test --workspaces --if-present"
  }
}
